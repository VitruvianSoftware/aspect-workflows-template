# BOOTSTRAP.md

1. Verify Aspect CLI is installed and accessible.
2. Run `bazel run tools:bazel_env` to verify the development environment setup.
3. Review `README.bazel.md` (if generated) for project-specific developer guidelines.